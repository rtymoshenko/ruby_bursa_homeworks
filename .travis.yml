language: ruby
rvm:
 - "2.2.0"
branches:
  only:
  - development
gemfile:
- Gemfile
before_script:
- psql -c 'create database shop_prod_db;' -U postgres
- bundle exec rake db:create db:migrate db:schema:load